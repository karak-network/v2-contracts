{
  "name": "@repo/contracts",
  "version": "0.0.0",
  "description": "",
  "scripts": {
    "build:dev": "forge build --ast && tsx buildTsArtifacts.ts",
    "clean": "forge clean && rm -rf ts-artifacts",
    "test": "forge test",
    "setup:contracts": "forge script script/v2/SetupCoreLocal.sol --broadcast --rpc-url http://localhost:8545 --code-size-limit 100000",
    "dev:setup": "pnpm setup:contracts",
    "build": "pnpm build:dev",
    "dev": "echo NOOP",
    "extractABI": "tsx extractABI.ts"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/node": "^20.11.24",
    "tsx": "^4.7.1"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2",
    "@openzeppelin/contracts-upgradeable": "^5.0.2",
    "solady": "^v0.0.201"
  }
}
